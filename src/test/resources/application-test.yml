spring:
  datasource:
    url: jdbc:h2:mem:monitoring;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  flyway:
    enabled: false

jwt:
  secret: test-secret-must-be-long-enough-for-hmac-sha256-hash
  expiration-ms: 3600000
